<!DOCTYPE html>
<html>
<head>
	<title>Rock or Not</title>
	<% include head %>
</head>
<body>
	<% include navbar %>
	<% if(name != undefined){ %>
		<h1>Welcome, <%=name %></h1>
	<% } %>

	<h1><%=band.title %></h1>
	<img src="/images/<%=band.imageUrl %>" />
	<div class="container">
		<div class="row">
			<div class="col-sm-12">
			</div>
		</div>
	</div>
</body>
</html>
